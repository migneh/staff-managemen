# 🔬 تشخيص ترتيب الأسبوع — ما الذي حدث فعلاً؟

> الصورة المرفقة أُعيد إنتاجها بالكامل من كود البوت (`score.js`, `reports.js`, `scheduler.js`). كل رقم أدناه مُثبت بتشغيل الكود، لا تقدير.

> ## ✅ حالة الإصلاح (2026-09-18)
>
> كل ما هو موصوف أدناه من خلل في الترتيب أُصلح: نافذة موحّدة 14 يوماً، استبعاد
> غير المتنشطين، قسم «غير مصنّف»، حدود لأفضل إداري، وإلغاء خصم -10 التلقائي.
> راجع [`CHANGELOG.md`](./CHANGELOG.md).

---

## ١) ما تكشفه الصورة من أول نظرة

| الملاحظة | السبب المؤكد |
|---|---|
| العنوان «(**أسبوعي**)» لكن الأرقام لا تتغير أسبوعياً | الترتيب يُحسب على **30 يوماً** لا 7: `reports.leaderboard(t)` بدون تمرير `days` فيأخذ الافتراضي `days = 30` (`reports.js:32`، `scheduler.js:237`). والـ footer في نفس الإيمبد يقول «آخر 30 يوم» — **الإيمبد يناقض نفسه** |
| **الجميع -10 نقاط** حرفياً | خصم `week_below_50` يُضاف لكل من Score < 50، **كل جمعة، بلا حد ولا تصفير** |
| ثلاثة Trial Moderators بنفس النتيجة **24** تماماً | هذا ليس تشابهاً — بل **قيمة افتراضية ثابتة** لأي شخص بلا نشاط |
| 🥇🥈🥉 لأشخاص بعدد مخالفات **0** | الترتيب لا يشترط حداً أدنى للمشاركة |

---

## ٢) من أين جاء الرقم 24؟

```
المخالفات المعالجة   = 5/30   ← السلم يعطي 5 كحد أدنى حتى مع صفر عمل
سرعة الاستجابة       = 10/25  ← ❌ لم يُقيَّم → يُمنح 10 تلقائياً (score.js:71)
التواجد والنشاط      = 5/25   ← صفر رسائل → 5 كحد أدنى
الالتزام             = 4/20   ← صفر أيام → 4 كحد أدنى
──────────────────────────────
المجموع = 24
```

**النتيجة:** السقف الأدنى الحقيقي للـ Score هو **24 وليس 0**. المقياس المضغوط في نطاق 24–100 يجعل «24» تبدو قريبة من «30» و«45»، والفرق بين شخص لا يعمل إطلاقاً وشخص يعمل قليلاً = **نقاط قليلة جداً**.

نفس المنطق يفسّر ترتيب الدعم: **Helper بصفر تكتات عند 45** مقابل **Support بـ32 تكتاً عند 59**. الفارق 14 نقطة فقط لأن:
- Helper بلا تقييم يحصل على **+10 تفاعل** و**+10 مشرف** = **20 نقطة مجانية من 50** (40% من درجته بلا أي قياس).
- Support بـ32 تكتاً: `tier(32) = 20/30` (لأن شرط الـ25 نقطة يبدأ من 35 تكتاً) + `10/25` للسرعة لأن **`avgRating` و`avgDuration` فارغان**.

مرة أخرى: **العوامل غير المُقيَّمة تُمنح نقاطاً افتراضية**، فيتقارب الجميع في منتصف المقياس بلا معنى.

---

## ٣) مشكلة أخطر: **حلقة الهبوط الأسبوعية**

```js
// scheduler.js:231-233 — كل جمعة
const sc = score.compute(m, score.monthlyRaw(m.user_id, 7)).score;
if (sc >= 80) points.add(m.user_id, 'week_above_80', ...);
else if (sc < 50) points.add(m.user_id, 'week_below_50', ...);   // ← -10 دائماً
```

ثلاث مشاكل متراكبة:

**أ) تراكم بلا تصفير** — مُوثَّق بالتشغيل:

| بعد الأسبوع | الرصيد |
|---|---|
| 1 | -10 |
| 2 | -20 |
| 4 | -40 |
| 10 | **-80** |

وعتبة `Helper → Support` هي **100 نقطة**. أي أن العضو الذي يمر بفترة ضعف يبدأ الترقية القادمة من **-80** ويحتاج **180 نقطة** ليصل. الخصم **يعاقب على نفس الأسبوع مرات لا نهائية**.

**ب) لا يوجد منع تكرار** — `ref_id` بلا قيد فريد:

```
نفس المرجع مرتين → -20 (لا قيد على (reason_key, ref_id))
```

فأي تشغيل مزدوج للمجدول (إعادة تشغيل البوت وقت التقرير، خطأ في المنطقة الزمنية — وهو موجود فعلاً كما في مراجعة الكود §2.1) يخصم مرتين.

**ج) العضو الجديد يُعاقب قبل أن يبدأ** — `probation` **ليس** ضمن المستثنين:

```
EXEMPT = ['on_leave','suspended','resigned']      // scheduler.js:17
الحالة: probation → مُستثنى من خصم الأسبوع؟ ❌ لا — سيأخذ -10
```

عضو عمره 3 أيام لم يقرأ قاعدة المعرفة بعد، يجد رصيده **-10**، ويظهر في الترتيب **-10** بجانب اسمه أمام الفريق. هذه أسرع طريقة لخسارة عضو جديد.

**د) وأثر جانبي نفسي:** الرقم `-10` يظهر في عمود «🎯» وكأنه تقييم، فيقرأه الفريق كحكم على شخصه، لا كعقوبة أسبوعية آلية.

---

## ٤) نتائج أخرى مُثبتة بالتشغيل

| المشكلة | الإثبات | الأثر |
|---|---|---|
| **«أفضل إداري بالشهر» بلا حد أدنى** — `scheduler.js:260` | `الأفضل سيُمنح لـ m1 بـ Score 24 و0 مخالفات (+50 نقطة)` | جائزة الشهر قد تذهب لمن لم يعمل، +50 نقطة تُبنى عليها ترقيات |
| **probation يُنافس المحترفين** — `reports.js:37` | `عضو probation عمره 3 أيام يظهر في الترتيب: ❌ نعم` | مقارنة غير عادلة لكليهما |
| **نافذتان زمنيتان في تقرير واحد** | الخصم على **7 أيام**، العرض على **30 يوماً** | يقول «أسبوعي» ويعاقب على أساس مختلف عمّا يعرض |
| **الحساب لا يُصفَّر بين الرتب** | `resetForNewRank` يعمل عند الترقية فقط | القيود السلبية تتبع العضو للأبد |

---

## ٥) الحل — بالترتيب

### أ) إيقاف النزيف فوراً (قبل أي تحليل)
الأهم أن **الخصم يعمل الآن وكل جمعة يزيد سوءاً**. حتى تتخذوا قراراً، أوقفوا التأثير السلبي فقط:

```js
// scheduler.js:233 — الأقل ضرراً: عدم الخصم مع الاستمرار بالتسجيل
else if (sc < 50) {
  // لا نخصم — النشاط المنخفض يُعاقب عليه بالمراجعة البشرية لا بالسالب المتراكم
}
```

### ب) الإصلاحات الصحيحة

| # | الإصلاح | الملف | الأثر |
|---|---|---|---|
| 1 | **استبعاد `probation`** من خصم الأسبوع ومن الترتيب (أو وسمه «تحت التأهيل») | `scheduler.js:17`, `reports.js:37` | لا عقاب قبل البدء |
| 2 | **قيد فريد** `UNIQUE(reason_key, ref_type, ref_id)` على `promotion_points` | `database.js` | يمنع الخصم المزدوج نهائياً |
| 3 | **حد أدنى للمشاركة في الترتيب** (مثلاً ≥ 5 أيام نشاط أو ≥ 5 عناصر عمل)، وإلا يُعرض في قسم «غير مصنّف» | `reports.js:33-43` | لا ميداليات لمن لم يعمل |
| 4 | **حد أدنى لـ«أفضل إداري بالشهر»**: Score ≥ 70 **و** ≥ 10 عناصر عمل | `scheduler.js:260` | الجائزة تعني شيئاً |
| 5 | **القيم الافتراضية**: العامل غير المُقيَّم لا يمنح نقاطاً، ويُحسب المقياس على **ما قيس فقط** (`pts / assessedMax × 100`) | `score.js:61-73` | «لم يُقيَّم» ≠ «جيد» |
| 6 | **توحيد النافذة**: أسبوعي = 7 أيام فعلاً، والـ footer يتبع المعامل | `scheduler.js:237`, `reports.js:31` | يتوقف التناقض |
| 7 | **السقف السالب**: لا تنزل النقاط تحت صفر العتبة الحالية (أو حد `max(-2×العتبة, ...)`) | `points.js` | لا حلقة هبوط |
| 8 | **توضيح العرض**: عنوان الترتيب يذكر النافذة صراحةً، وعمود «🎯» يُسمّى «نقاط الرتبة الحالية» | `reports.js:31` | يقل سوء الفهم |

---

## ٦) قرارات تحتاجونها (سياسة، لا كود)

1. **هل نريد خصماً أسبوعياً أصلاً؟** التعليم بالخصم المتكرر ينتج عنه **انسحاب** لا تحسّن. البديل: لا خصم، بل ظهور تلقائي في مراجعة الإدارة + مهمة متابعة.
2. **هل تُصفَّر النقاط عند كل رتبة؟** (حالياً تُصفَّر عند الترقية، فتبدأ كل رتبة من الصفر — لكن السلبيات تتراكم قبلها).
3. **هل يُعرض الترتيب للفريق كله أم للإدارة؟** ترتيب علني لأشخاص بلا نشاط يُحرجهم؛ الأفضل «أفضل 5 + مقارنة ذاتية» أو إخفاء الأسماء لمن دون الحد الأدنى.
4. **نافذة الأسبوعي**: 7 أيام (متقلبة جداً في فرق صغيرة) أم 14 يوماً (أعدل)؟
5. **حد أدنى للتقييم**: هل يُشترط عدد تقييمات أدنى (مثلاً 10) قبل أن يُحتسب عامل «تقييم العملاء»؟

---

## ٧) أهم استنتاج

الترتيب الحالي **يقيس بشكل سيئ** لا أنه «يحتاج تحسيناً تجميلياً»:

- **لا يميّز** بين صفر عمل وقليل عمل (24 مقابل 30).
- **يعاقب أكثر مما يكافئ** (خصم أسبوعي تراكمي بلا سقف).
- **يكافئ الغياب عن القياس** (10 نقاط مجانية لمن لم يُقيَّم).
- **يمنح ميداليات وجوائز شهرية بلا حد أدنى**.

هذه الأربعة مجتمعة تُنتج بالضبط ما في الصورة: **قائمة بأرقام متشابهة، ميداليات بلا معنى، وكل الفريق برصيد سالب.**

> الإصلاح لا يحتاج إعادة بناء: **٥ تعديلات صغيرة** (probation مستثنى، القيم الافتراضية صفر، حد أدنى للترتيب وللجائزة، قيد فريد للتكرار) تُخرج النتيجة من «بيانات مشوّهة» إلى «ترتيب يمكن الاعتماد عليه».
