# سجل التغييرات

## [1.1.0] — 2026-09-18

إصلاحات شاملة مبنية على مراجعة الكود: صحّة الأرقام، أمان الصلاحيات، وموثوقية المهام المجدولة.

### أُضيف

- `src/clock.js` — مصدر واحد للوقت والتواريخ: «التاريخ» بتوقيت `TZ` (الإجازات، الحدود
  اليومية، التقارير) و«الطابع الزمني» بـ UTC كما تخزّنه SQLite. كل دوال التواريخ في
  `utils.js` صارت تعيد التصدير من هنا.
- `src/logger.js` — سجل موحّد بمستويات (info/warn/error) و`setMirror` لعكس التحذيرات
  إلى قناة `staff-logs` دون إسقاط البوت.
- `/unsuspend` — رفع الإيقاف يدوياً (Boss)، ومهمة مجدولة يومية ترفع الإيقاف المنتهي
  تلقائياً (`staff_members.suspended_until`).
- `/system-status` — صحة النظام: آخر تشغيل لكل مهمة مجدولة (`job_runs`)، أحجام الجداول،
  آخر نسخة احتياطية، حالة الإعداد.
- جدول `promotion_approvals` — نصاب موافقات الترقية (صوت واحد لكل شخص لكل طلب).
- أعمدة جديدة: `ticket_metrics.duration_source` و`claimed_at`،
  `staff_tasks.cancelled_by` و`cancelled_at`، `staff_members.suspended_until`.
- فهارس: `idx_activity_created`, `idx_job_runs_job`, ومؤشر فريد جزئي يمنع تكرار نقاط
  نفس المرجع في `promotion_points`.
- أدوات: `npm run lint` (ESLint مسطّح)، `npm run validate` (بوابة سجل الأوامر والمكوّنات)،
  `npm run docs:commands` (يولّد جدول أوامر README من الكود)،
  `npm run migrate:tz -- --dry-run` (ترحيل أيام النشاط القديمة)، وسير عمل CI في
  `.github/workflows/ci.yml`.

### أُصلح

- **الصلاحيات**: فرض مركزي لكل مكوّن (زر/قائمة/نموذج) عبر خريطة `COMPONENT_ACCESS`
  (84 مكوّناً). ثلاثة معالجات كانت مفتوحة لأي إداري: `promo:reject`, `promo:rejectmodal`,
  `faq:hist` — إضافة إلى `leave:pending` و`leave:details`. كما صار المستوى الفعلي
  محسوباً قبل التنفيذ بدل الاعتماد على فحص كل معالج لنفسه.
- **Score**: الرسائل تُحتسب بوزن قناتها (كان الوزن مكتوباً في الإعدادات ولا يُقرأ أبداً)،
  والعوامل غير المُقيَّمة (تقييم المشرف/سرعة الاستجابة) تُستثنى من المقام بدل منح نقاط
  مجانية، والإجاريون بلا تقييم لم يعودوا يحصلون على 100 تلقائياً.
- **الـ Leaderboard**: استبعاد `on_leave`/`suspended`/`resigned`/`removed`/`on_probation`
  من الترتيب (كان المجاز ينافس العاملين)، ونافذة موحّدة 14 يوماً، وحد أدنى 3 أيام نشاط
  أو عنصر عمل ليظهر الاسم في الترتيب، وإلا يظهر في قسم «غير مصنّف» مع السبب.
- **أفضل إداري بالشهر**: يشترط Score ≥ 70 وعشرة عناصر عمل على الأقل — لا لقب لمن لم يعمل.
- **الخصم التلقائي (-10)**: أُلغي؛ الأسبوع الضعيف (Score < 50) يُدرج في «قائمة مراجعة
  بشرية» داخل التقرير الأسبوعي، والعضو في فترة التجرية معفى.
- **ترقيات**: منح الموافقات نصاباً فعلياً (`approvals` في القواعد)، ونوافذ زمنية موحّدة
  لكل شرط (`windowDays`/`warnWindowDays`) بدل «منذ بداية الرتبة»، وشرط تقييم المشرف صار
  مانعاً حقيقياً بدل تجاهله، وشرط التواجد مبني على `activeDays` بدل عدّ سجلات عشوائي.
- **المهام المجدولة**: حارس يمنع تشغيل نسختين متزامنتين من المهمة نفسها (فحص الغياب كل
  30 دقيقة قد يتجاوز مدته)، وتسجيل كل تشغيل في `job_runs`، وإغلاق لطيف عند SIGINT/SIGTERM
  يوقف المجدول ويغلق قاعدة البيانات.
- **سجل التكتات**: مدة الحل تُحلَّل من «HH:MM» أو «ساعات/دقائق» إلى دقائق (كانت أول رقم في
  النص، فـ «01:23» تصبح دقيقة واحدة)، وإن لم يذكرها البوت فتُحسب من فرق الوقت بين رسالة
  الاستلام ورسالة الإغلاق.
- **استقرار**: معالجة `unhandledRejection`/`uncaughtException` بالتسجيل بدل الإسقاط،
  وقاعدة البيانات صارت تتقبّل قيماً تالفة في الإعدادات (`safeParse`) بدل الانهيار،
  وتسجيل نص فارغ في قناة غير مربوطة لم يعد يُسكت الخطأ بصمت.
- **مغادرة الفريق**: نزع الرتب الإدارية يدوياً أو مغادرة السيرفر تُحدّث الحالة إلى
  `removed` (كان العضو يبقى «نشطاً» في التقارير إلى الأبد).
- تنظيف: `CHANNEL_META` مصدر واحد (كان منسوخاً في `setup.js` بحجم 1053 حرفاً)، وإزالة
  شروط ميتة (`shouldAdd ? true : true`، وكتل `else` فارغة)، وإصلاح `divider` غير المستورد
  في `/staff-report` الذي كان يرمي استثناء عند كل استخدام.

### أُضيف للاختبارات

21 اختباراً تمر كلها، تشمل: دلالات Score الجديدة (المقام المُقيَّم)، نصاب الترقية والنوافذ،
حدود الـ Leaderboard و«غير مصنّف»، تحليل المدة بأشكالها، وراوتر المكوّنات.

## [1.0.0]

الإصدار الأول: نظام إدارة فريق كامل (تكتات، نشاط، تقارير، إجازات، استقالات، ترقيات، FAQ).
